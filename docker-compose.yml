services:
  web:
    build: .
    container_name: contemporary-web
    environment:
      NEXT_PUBLIC_API_BASE: http://localhost:5000/api
    restart: unless-stopped
    # optional: remove host port if you only need internal access
    ports:
      - '3000:3000'
    